package backend;

/**
 * Represents a position on the grid and can be anywhere within a subgrid.
 * It has all the characteristics of a node.
 */
public class Position extends Node {
    public Position(double x, double y) {
        super(x, y);
    }
}
