package backend;

/**
 * Represents an entrance along the edges of a subgrid in a grid.
 * Has all the characteristics of a node.
 */
public class Entrance extends Node {
    public Entrance(double x, double y) {
        super(x, y);
    }
}
